 /*           string numberToolOne = Console.ReadLine();
            int numberSize = Convert.ToInt32(numberToolOne);
            int[] numbers = new int[numberSize];

            for (int i = 0; i < numbers.Length; i++)
            {
                numbers[i] = Convert.ToInt32(Console.ReadLine());
                
            }
            int max = numbers[0];
            for (int i = 1; i < numbers.Length; i++)
            {
                if (numbers[i] >= max) 
                {
                    max = numbers[i];
                }
            }
            Console.WriteLine(max);*/
            
            
            
            
            
            /* string lectureTool = Console.ReadLine();
            int lectureSize = Convert.ToInt32(lectureTool);
            string[] lecture = new string[lectureSize];
            int maxWordSize = 0;
            string maxWord = "";
            for (int i = 0; i < lectureSize; i++)
            {
                lecture[i] = Console.ReadLine();
            }
            for (int i = 0; i < lecture.Length; i++)
            {
                if (lecture[i].Length>maxWordSize)
                {
                    maxWordSize = lecture[i].Length;
                    maxWord = lecture[i];
                }
            }
            Console.WriteLine(maxWord);*/
